import type { ImageComponentProps } from "../types/components";

export const imageData: ImageComponentProps = {
    id: Date.now().toString(),
    type: 'image',
    url: '',
    align: 'center',
    size: 250,
    redirectUrl: '',
    background: 'transparent'
}